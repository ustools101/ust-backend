<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browser Compatibility Notice</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background: linear-gradient(135deg, #f5f7fa 0%, #e4e9f2 100%);
            padding: 20px;
            line-height: 1.6;
        }

        .message {
            text-align: center;
            padding: 2.5rem;
            background-color: white;
            color: #1a1a1a;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.08);
            max-width: 90%;
            width: 500px;
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255,255,255,0.2);
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            color: #1a1a1a;
            line-height: 1.3;
        }

        @media (max-width: 480px) {
            h1 {
                font-size: 1.25rem;
            }
            .message {
                padding: 1.5rem;
            }
        }

        .copy-button {
            background: linear-gradient(135deg, #4b6cb7 0%, #182848 100%);
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 12px;
            cursor: pointer;
            margin: 20px 0;
            font-size: 1rem;
            font-weight: 500;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            width: 200px;
        }

        .copy-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(75, 108, 183, 0.2);
        }

        .copy-button:active {
            transform: translateY(0);
        }

        .copy-button.copied {
            background: linear-gradient(135deg, #43A047 0%, #2E7D32 100%);
        }

        .instruction {
            font-size: 0.95rem;
            color: #666;
            margin-top: 15px;
            padding: 0 10px;
            opacity: 0.9;
        }

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 10px;
        }

        .icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            color: #4b6cb7;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }
    </style>
</head>
<body>
    <div class="message fade-in">
        <div class="container">
            <div class="icon">🌐</div>
            <h1>Please open the website with Safari, Chrome or any other browser</h1>
            <button class="copy-button" onclick="copyCurrentURL()">Copy Link</button>
            <p class="instruction">After copying the link, please close Instagram's browser and paste the link in Safari, Chrome, or another browser.</p>
        </div>
    </div>

    <script>
        function copyCurrentURL() {
            const currentURL = window.location.href;
            const button = document.querySelector('.copy-button');
            
            navigator.clipboard.writeText(currentURL).then(() => {
                const originalText = button.textContent;
                button.textContent = 'Link Copied! ✓';
                button.classList.add('copied');
            }).catch(err => {
                console.error('Failed to copy URL:', err);
                button.textContent = 'Failed to copy';
                setTimeout(() => {
                    button.textContent = 'Copy Link';
                }, 2000);
            });
        }
    </script>
</body>
</html>